version: "3"

services: 
  react-app:
    build:
      context: .
      dockerfile: Dockerfile.dev
    ports:
      - "80:443"
    volumes:
      - ./src:/src/app
    environment:
     - REACT_APP_NAME=ThePICARD
     - CHOKIDAR_USEPOLLING=true
